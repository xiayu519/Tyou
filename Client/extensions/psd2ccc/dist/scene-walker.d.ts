export declare const methods: {
    buildNodes(uiNodeName: string, jsonStr: string, spriteMapStr: string): {
        success: boolean;
        count: number;
        rootUuid: any;
        rootName: any;
    };
};
