## 1. PSD 导出去重

- [x] 1.1 将 `Psd2CCC-Digest.jsx` 的普通图层去重从直方图指纹改为导出 PNG 精确内容指纹
- [x] 1.2 将 `.img` 组合导出的去重同步改为导出 PNG 精确内容指纹

## 2. 验证

- [x] 2.1 静态检查脚本中不再使用直方图作为普通 PNG 去重依据
- [x] 2.2 用现有师祖赐福 PSD/PNG 数据验证 `img_masterlove_once` 与 `img_masterlove_five` 是不同内容，说明修复目标明确

## 3. 实测反馈修正

- [x] 3.1 将整 PNG 二进制指纹改为 PNG 图像内容指纹，排除时间/文本等非图像元数据
- [x] 3.2 静态检查普通图层和 `.img` 组合导出都使用 PNG 图像内容指纹

## 4. 透明像素归一化修正

- [x] 4.1 将 PNG 去重从压缩图像 chunk 指纹改为黑/白底合成后的可见像素指纹
- [x] 4.2 将九宫格 `sliceBorder` 纳入去重指纹，避免同图不同九宫边界误复用
- [x] 4.3 验证 `TestPsd_HB1_bg1_6c2b` 与 `TestPsd_HB1_Window_bg_6eb9` 这种仅透明像素 RGB 不同的九宫格可以复用
