## 1. Workflow Documentation

- [x] 1.1 Update `AGENTS.md` to describe `rg` as preferred and list fallback search tools.
- [x] 1.2 Update `.agents/skills/tyou-dev/references/workflow-recovery.md` with the same fallback rule.
- [x] 1.3 Update `.agents/skills/tyou-dev/SKILL.md` with the same fallback rule.
- [x] 1.4 Update `Books/AI-Development-Workflow.md` with the same fallback rule.

## 2. OpenSpec Sync

- [x] 2.1 Apply the `codex-ai-workflow` fallback requirement to the main OpenSpec spec.

## 3. Validation

- [x] 3.1 Verify OpenSpec status and confirm the existing memory record does not need duplication.