# Proposal

## Why

The developer confirmed this is a new framework project and requested deletion of prior memory entries so the optimized memory workflow starts from a clean baseline.

## What

- Remove existing active memory entries created before the new memory workflow baseline.
- Keep `.codex/memory/INDEX.md`, typed memory directories, `.gitkeep` files, memory rules, and templates.
- Update the memory index to show an empty baseline.
- Validate the full Codex workflow after the reset.

## Non-Goals

- Do not delete memory rules or templates.
- Do not alter archived OpenSpec history.
- Do not reintroduce visualization or automatic memory extraction.
