# Proposal

## Why

The project workflow should describe the current Codex operating model directly. Repository files should carry current executable rules and concise evidence paths.

## What

- Rewrite active workflow docs and specs into positive current-state wording.
- Keep current Codex entrypoints and remove obsolete archive records.
- Keep the current memory baseline and structured memory rules.
- Verify the repository contains current workflow wording in active workflow files.

## Non-Goals

- Do not change Cocos runtime code, assets, Prefab/Scene/meta, Luban data, or generated config.
- Do not remove current Codex rules, skills, memory templates, OpenSpec specs, or sensor scripts.
