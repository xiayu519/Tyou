## 1. Streamline Specs And Wording

- [x] 1.1 Merge duplicated project memory requirements in `codex-ai-workflow` without changing memory behavior.
- [x] 1.2 Replace remaining legacy `rules` wording with `references` where it does not refer to official `.rules` command policy.

## 2. Streamline OpenSpec Skills

- [x] 2.1 Move detailed `openspec-explore` procedure text into a skill reference while preserving name, description, and behavior.
- [x] 2.2 Move detailed `openspec-propose` procedure text into a skill reference while preserving name, description, and behavior.
- [x] 2.3 Move detailed `openspec-apply-change` procedure text into a skill reference while preserving name, description, and behavior.
- [x] 2.4 Move detailed `openspec-archive-change` procedure text into a skill reference while preserving name, description, and behavior.

## 3. Validate And Archive

- [x] 3.1 Run OpenSpec validation and wiki-sync scan/diff.
- [x] 3.2 Run static checks for preserved OpenSpec trigger descriptions and reduced skill entrypoint size.
- [x] 3.3 Record validation evidence in `run-report.md` and run observability sensor.
- [x] 3.4 Archive the completed change when gates are green.
