## 1. Resource API

- [x] 1.1 Add a `ResourceModule` API for loading Spine from a specified bundle/version as `sp.SkeletonData`.
- [x] 1.2 Keep existing `loadSpineAsync` and `loadSpineEffectAsync` behavior unchanged.

## 2. UIBase Helpers

- [x] 2.1 Add UIBase helpers for assigning remote bundle Spine and remote bundle Spine effects.
- [x] 2.2 Preserve owner epoch, per-target request ordering, target validity checks, and automatic release.

## 3. Validation

- [x] 3.1 Run TypeScript validation or the closest available static check.
- [x] 3.2 Validate the OpenSpec change status.
