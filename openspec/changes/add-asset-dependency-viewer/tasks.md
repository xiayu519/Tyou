## 1. 扩展脚手架

- [x] 1.1 创建 `asset-dependency-viewer` package、TypeScript 配置、入口和面板贡献声明
- [x] 1.2 添加可独立运行的数据模型与归并工具测试入口

## 2. 依赖索引与刷新

- [x] 2.1 实现 AssetDB 快照查询、UUID 图构建、子资源路径归并和双向去重计数
- [x] 2.2 实现防抖、单 in-flight、dirty 追加刷新、超时和旧快照保留
- [x] 2.3 接入 AssetDB ready/add/change/delete 广播与手动刷新消息

## 3. 编辑器交互

- [x] 3.1 实现 dockable 总览面板、搜索、状态栏和窗口化资源列表
- [x] 3.2 实现 simple 详情面板、双向完整路径列表和资源跳转
- [x] 3.3 实现 Tools 菜单、Assets 右键入口以及打开在 Assets 旁边的行为

## 4. 验证与交付

- [x] 4.1 安装依赖并完成 TypeScript 构建、核心模型测试和扩展清单校验
- [x] 4.2 完成 run-report、可观测性 sensor、OpenSpec 状态和文档/memory/归档自检

## 5. Creator 首次查询超时修复

- [x] 5.1 将资源枚举改为 Creator 3.8.7 已验证的无参数 `query-assets` 调用，并把依赖补齐放到受限并发阶段
- [x] 5.2 调整启动失败降级与日志级别，避免可恢复超时产生红色错误和连续自动重试
- [x] 5.3 增加慢查询/超时回归测试，重新构建并更新 run-report 与 sensor

## 6. 显式启停与缓存生命周期

- [x] 6.1 实现默认关闭、显式开启、缓存恢复/保存、显式关闭删除缓存的控制器
- [x] 6.2 更新 Tools 菜单、总览状态和按钮，确保打开面板不会隐式扫描
- [x] 6.3 增加默认关闭、开启扫描、资源变化刷新、关闭零查询和缓存恢复测试

## 7. 分类与冗余视图

- [x] 7.1 在路径级依赖图中标记资源/脚本分类，并实现 `userCount === 0` 的冗余筛选模型
- [x] 7.2 在总览面板实现 `资源引用/冗余资源` 与 `资源/脚本` 两级 toggle、独立计数和搜索
- [x] 7.3 增加分类/冗余回归测试，重新构建并更新 run-report 与 sensor

## 8. 全局脚本、启用状态与目录范围

- [x] 8.1 使用项目级 Profile 独立持久化 enabled 状态，并处理有状态无缓存的启动恢复
- [x] 8.2 实现 TypeScript AST 全局提供者/消费者分析、补边和 Global 冗余保护
- [x] 8.3 实现 Assets 文件夹右键范围、当前选择范围、范围面包屑和清除范围
- [x] 8.4 增加状态恢复、全局脚本和目录范围回归测试，重新构建并更新 run-report 与 sensor
