# 2026-05-09 问题记录

## rg 不可用

- 现象：执行 `rg "..."` 时 PowerShell 报 `无法将“rg”项识别为 cmdlet、函数、脚本文件或可运行程序的名称`。
- 定位：当前 Windows 环境未安装 ripgrep 或未加入 PATH。
- 根因：AI 通用习惯优先使用 `rg`，但本机项目环境暂不可用。
- 修正动作：本会话改用 VS Code `grep_search` 进行文本搜索；后续如需命令行搜索，先确认 `rg` 是否可用，或直接使用 `grep_search`。
