# Tyou project defaults. Allowed reasoning is medium/high/xhigh/max; default high, Plan mode xhigh.
# max is explicit-only for the hardest quality-first tasks or evals; Tyou Deep is a separate risk label.
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
plan_mode_reasoning_effort = "xhigh"
